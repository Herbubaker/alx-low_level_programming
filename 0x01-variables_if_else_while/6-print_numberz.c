#include <stdio.h>

/**
 * main - Entry Point
 * numbers between 0 and 9
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int b;

	for (b = 48; b < 58; b++)
	{
		putchar(b);
	}
	putchar('\n');
	return (0);
}
