#include <stdio.h>

/**
 * main - Entry point
 * numbers between 0 and 9
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int b;

	for (b = 0; b < 10; b++)
		printf("%d", b);
	printf("\n");

	return (0);
}
